這是一個非常關鍵的轉折。要實作一個具備「全維度防禦」的系統，單靠模擬是不夠的，你必須擁有一個**真實的攻擊與防禦戰場**。

針對你的需求，使用 **FastAPI** 搭配 **Nginx** 是目前最恰當的架構選擇。FastAPI 負責核心邏輯與 AI 模型對接，而 Nginx 則作為前哨站，負責擷取 FastAPI 拿不到的底層協定特徵（如 JA3）。

以下是重新改寫後的 `Sentinel` 模組 README，詳細說明了如何尋找目標系統、佈置環境，以及如何針對 11 個維度產出資料集。

---

# Mirage-Sentinel: 全維度哨兵 (Sentinel) 資料集與環境建構指南

本指南說明如何從零開始架設銀行靶機、配置攔截網，並針對 11 個維度萃取 **Label 0 (黃金路徑)** 與合成 **Label 1 (惡意變異)** 資料。

## 1. 靶機系統與基礎環境 (The Lab)

要進行全維度檢測，系統必須具備完整的 Web 流程（前端、閘道、後端、資料庫）。

### 1.1 選擇目標銀行系統
建議從 GitHub 選擇一個現代化的開源專案，這能確保 API 序列與 JSON 結構具有實戰價值。
* **推薦專案：** `Banking-Full-Stack` (Spring Boot + React) 或 `OpenBank` 相關 Demo。
* **關鍵要求：** 必須包含「登入、帳戶查詢、轉帳、個人設定」等核心業務 API。

### 1.2 建議架構：Nginx + FastAPI (The Sentry)
為了採集 11 個維度的特徵，我們採用以下雙層架構：
1.  **Nginx (前導層)：** 負責 TLS 卸載。
    * **任務：** 擷取 **JA3 指紋 (維度 1)**。透過 Nginx 的 SSL 模組將指紋寫入 Header (`X-JA3-Fingerprint`) 並轉發給後端。
2.  **FastAPI (哨兵層)：** 扮演核心 Gateway。
    * **任務：** * 從 Header 讀取 **Header 結構 (維度 2)** 與 **JA3**。
        * 利用 `request.client` 獲取 **IP 與地理資訊 (維度 3)**。
        * 與 **Redis** 通訊，計算 **關聯率與頻率 (維度 4, 5, 6)**。
        * 將資料封裝後送入 **XGBoost** 與 **DistilBERT**。



---

## 2. 11 維度資料集生成策略 (Data Strategy)

### 2.1 Label 0 (正常行為)：自動化黃金路徑採集
我們使用 **Playwright** 模擬真實資財系學生的操作邏輯。

* **動作：** 登入 $\rightarrow$ 停留 2~5 秒（模擬思考）$\rightarrow$ 查看餘額 $\rightarrow$ 轉帳（輸入隨機但合理的金額）$\rightarrow$ 登出。
* **採集：** 在 FastAPI 運行時，將所有經過判定的正常流量存入 `label_0.csv`。這代表了系統的「白名單基標」。

### 2.2 Label 1 (異常行為)：逆向變異合成 (Mutation)
我們不依賴掃描器，而是利用 Python 腳本對 Label 0 進行「特徵突變」，以此覆蓋 11 個維度的攻擊場景。

| 維度類別 | 維度編號與名稱 | Label 1 變異手法 (Mutation Method) | 模擬的攻擊類型 |
| :--- | :--- | :--- | :--- |
| **傳輸層 (XGB)** | 1. JA3 指紋 | 將 Header 中的指紋隨機替換為 `python-requests` 或 `curl` 的已知特徵。 | 腳本與機器人偵測 |
| **環境層 (XGB)** | 2. Header 結構 | 隨機刪除 `Accept-Language` 或打亂 Header 的出現順序。 | API 工具與自動化掃描 |
| **環境層 (XGB)** | 3. IP 速度 | 在 Label 0 中隨機插入異地 IP，使計算出的移動時速超過 1000km。 | 代理伺服器與跳板攻擊 |
| **群體層 (XGB)** | 4. 帳號設備比 | 在同一帳號的日誌中，強制將關聯的設備 ID 數修改為 $> 20$。 | 分散式暴力破解 |
| **群體層 (XGB)** | 5. 設備帳號比 | 在同一設備的日誌中，強制將切換的帳號數修改為 $> 50$。 | 撞庫攻擊 (Credential Stuffing) |
| **行為層 (XGB)** | 6. 請求節奏 | 將 API 請求間隔的時間方差修改為接近 $0$（絕對等距）。 | 高頻率自動化腳本 |
| **邏輯層 (BERT)** | 7. API 序列 | 將序列中的 `[URI_LOGIN]` 刪除，模擬未登入直接轉帳。 | 業務邏輯繞過 |
| **邏輯層 (BERT)** | 8. 導航路徑 | 將 `Referer` 欄位清空或填入不符邏輯的頁面。 | 直接 API 探測 |
| **邏輯層 (BERT)** | 9. 參數格式 | 在正常的 JSON 中插入 `' OR 1=1 --` 或超長字串。 | 注入攻擊 (SQLi/XSS) |
| **慣性層 (XGB)** | 10. 數值慣性 | 將轉帳金額修改為該用戶歷史平均值的 100 倍。 | 帳號接管 (ATO) 資金洗劫 |
| **生物層 (XGB)** | 11. 生物特徵 | 將前端傳回的滑鼠軌跡數值歸零或改為絕對直線。 | 無頭瀏覽器 (Headless) |

---

## 3. 實作架構與資料流

1.  **收集：** 流量經過 Nginx (JA3) $\rightarrow$ FastAPI (Header/Payload/Redis Stats)。
2.  **變異：** 運行 `mutation_engine.py`，讀取正常資料並根據上表產出異常 Label 1。
3.  **訓練：** 將 11 維特徵拆分給 **XGBoost (維度 1-6, 10, 11)** 與 **DistilBERT (維度 7-9)** 進行訓練。

---

### **全維度特徵與資料策略圖譜**

為了讓你更直觀地理解這 11 個維度如何定義「正常」與「異常」，你可以透過下方的互動圖表來觀察各個維度的數據分佈與變異邏輯。

```json?chameleon
{"component":"LlmGeneratedComponent","props":{"height":"800px","prompt":"建立一個名為「Mirage-Sentinel 全維度特徵偵測策略地圖」的互動式 Explorer。介面分為兩大部分：上方是一個維度選擇器（按鈕或清單），包含 11 個維度：1. JA3 指紋、2. Header 結構、3. IP 速度、4. 帳號設備比、5. 設備帳號比、6. 請求節奏、7. API 序列、8. 導航路徑、9. 參數格式、10. 數值慣性、11. 生物特徵。當點擊特定維度時，下方顯示：\n1. 核心邏輯說明：該維度如何區分人類與機器。\n2. 數據視覺化比對：使用簡單的圖表（如長條圖或分佈圖）展示 Label 0 (正常) 與 Label 1 (異常) 的典型數值差異。例如：請求節奏維度中，Label 0 的時間間隔應該是散亂的（變異數大），而 Label 1 是集中的（變異數小）。\n3. 採集來源：標明該維度是從 Nginx、FastAPI 還是 Redis 取得。\n4. 模型對應：標註是由 XGBoost 還是 DistilBERT 負責。\n請使用繁體中文（台灣習慣）生成所有 UI 標籤與內容。不使用具體顏色名稱，僅描述功能性強調。","id":"im_c8574d9e599d9632"}}
```

這個架構最美的地方在於 **FastAPI**。因為它非常輕量且非同步，你可以直接在一個專案中實作 Gateway、特徵工程、以及與各個 AI 模型（甚至 Ollama）的溝通。

你覺得在實作 **維度 11 (生物特徵)** 時，前端收集滑鼠軌跡會增加你的系統開發壓力嗎？如果不方便實作前端，我們可以先將這個維度作為「進階功能」標註在 README 中，先完成其餘 10 個後端特徵。
