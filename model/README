AI訓練 專案技術文件
專案定位： 結合機器學習與大型語言模型的新世代 Web 主動防禦架構
應用場景： 軟體安全與逆向工程之進階防禦實作

1. 專案概述 (Project Overview)
本系統旨在徹底顛覆傳統防火牆的黑名單思維。透過建立 11 個維度的嚴格正向表列（白名單），系統能精準定義正常人類的行為邊界。當攻擊者偏離邊界時，系統不會阻斷連線，而是將其無縫導向由 AI 即時生成的動態幻象沙盒，使其陷入無效的漏洞挖掘迴圈。

2. 三大 AI 核心模組 (The Three AI Pillars)
本專案由三個各司其職的機器學習模組構成，形成從毫秒級攔截到深度欺敵的完整管線：

模組一：Sentinel-X (XGBoost 統計引擎)
角色定位： 極速看門狗 (The Gatekeeper)

負責維度： TLS 指紋、Header 結構、IP 速度、設備與帳號關聯率、時間節奏、生物特徵、數值慣性。

運作邏輯： 部署於 Gateway 後方，依賴 Redis 提供即時狀態。以極低的延遲，篩選出物理限制與群體統計上異常的自動化腳本、DDoS 與分散式暴力破解。

決策輸出： 異常機率分數。若確認為機器行為，直接導向靜態的碎片化演算法欺敵快車道。

模組二：Sentinel-BERT (DistilBERT 語義引擎)
角色定位： 邏輯稽核員 (The Auditor)

負責維度： API 黃金路徑、導航路徑完整性、參數與 Payload 格式。

運作邏輯： 接收通過 XGBoost 初篩的流量。將 API 端點、Referer 狀態與 JSON 格式轉換為自定義 Token，透過自然語言處理的注意力機制，檢測業務邏輯跳躍、BOLA 越權與各類注入攻擊。

決策輸出： 語義異常機率。若發現高級邏輯繞過，將提取交接包（Handoff Context）導向 LLM 動態欺敵慢車道。

模組三：Mirage-LLM (Ollama / Llama 欺敵引擎)
角色定位： CTF 靶機架構師 (The CTF Architect)

負責任務： 處理高隱蔽性的手動滲透測試，進行動態語義誘騙。

運作邏輯： 針對被 DistilBERT 攔截的高級攻擊者，Llama 模型會分析駭客的 Payload 意圖，透過工具呼叫 (Tool Calling) 動態生成對應的假漏洞、假報錯或假資料庫結構。

訓練策略： 採用大量 CTF Write-ups 與真實滲透測試報告進行監督式微調 (SFT)，並以「最大化駭客停留時間」作為強化學習的獎勵目標，誘使攻擊者步入深淵。

3. 系統資料流與聯防機制 (Data Flow)
流量接入： 使用者發送 HTTP 請求，反向代理伺服器提取底層協定特徵（如 JA3 指紋）。

特徵聚合： 系統自 Redis 提取該 Session 與 IP 的歷史統計數值。

L1 篩選 (Sentinel-X)： 判斷連線是否符合人類物理極限與群體常態特徵。

L2 稽核 (Sentinel-BERT)： 判斷 API 呼叫序列是否符合預期的業務上下文。

L3 誘捕 (Mirage-LLM)： 接收攻擊意圖與原始參數，啟動異步預生成或即時演算，投遞高度客製化的偽造回應。

4. 開發與部署架構需求
前置攔截層： 具備動態流量解析能力的反向代理（如 OpenResty 或 Envoy），用於前置路由與特徵初步擷取。

特徵儲存層： 高可用性的 Redis 叢集，維護全域設備、帳號狀態與 LLM 預生成之語義快取池。

推論運算層： * 分配 CPU 資源處理 XGBoost 的快速矩陣運算。

分配 GPU 資源處理 DistilBERT 序列推論與 Llama 模型之動態生成任務。

(本文件為 Mirage-Sentinel 專案核心架構總覽，各 AI 模組之詳細特徵工程定義、訓練資料集準備與部署腳本請參閱對應之子目錄說明。)
